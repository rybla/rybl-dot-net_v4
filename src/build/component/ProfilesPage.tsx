import type { Ctx } from "@/ef";
import type { PromiseElement, Website } from "@/ontology";

export default async function ProfilesPage(props: {
  ctx: Ctx.T;
  website: Website;
}): PromiseElement {
  return <div>TODO: ProfilesPage</div>;
}
